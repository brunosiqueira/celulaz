<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "Painel", user_url  %></li>
      <li><%= link_to "Usu&aacute;rios", users_path  %></li>
      <li><%=link_to "Criar Usuário Comercial", :action=>:new %></li>
      <li><%=link_to "Criar #{I18n.t("company.singular").capitalize}", :action=>:new,:type=>UserCompany.name %></li>
    </ul>
  </div>
  <div class="content">
    <div class="inner">
      <h1>Ambiente Administrativo</h1>
      <div class="admin-info">
        <%= image_tag(current_user.avatar.url(:small)) %>
        <div>
          <p>Seja bem vindo, <strong><%=h @user.login %></strong>. <br />
			Voc&ecirc; est&aacute; cadastro desde <%= @user.created_at.to_s_br %>.</p>
        </div>
      </div>
      <%= render :partial=>"people/person_show",:object=>@user.person %>
      <% if @user.is_company? %>
        <h2><%=I18n.t "company.header"%></h2>
        <%= render :partial=>"companies/company_show",:object=>@user.company %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, render(:partial => 'sidebar') -%>  