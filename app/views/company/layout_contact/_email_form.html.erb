<div id="form_space5" class="email-form" style="display:none;">
	<% form_for :layout, @company.layout, :url => {:controller=>"company/layout_contact", :action=>"update_component"}, :html => { :class => "edit_layout_email"} do |f|%>
		<%= hidden_field_tag "component_type", "email" %>
		<%= f.text_field :email, :class => 'text_field',:style=>"width:280px"%>
		<div class="actions">
			<%= f.submit "Salvar" %>
		    <a href="#">Cancelar</a>
		</div>
	<% end %>				
</div>